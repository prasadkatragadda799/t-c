{
  "name": "t-and-c",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
